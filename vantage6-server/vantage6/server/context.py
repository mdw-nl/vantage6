# Keep these imports as compatibility re-exports for existing server callers
# while the implementations live in vantage6.common.
from vantage6.common.server_context import BaseServerContext, ServerContext
